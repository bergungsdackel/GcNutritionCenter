<Window x:Class="GcNutritionCenter.CustomDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen" 
        SizeToContent="WidthAndHeight"
        Width="300"
        Height="300"
        WindowStyle="None">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="15">
            <Label Name="lblQuestion" Content="TEST123"/>
            <TextBox Name="txtInput" MinWidth="250" Text="TEST321" Margin="0,5,0,0"/>
        </StackPanel>


        <Grid Grid.Row="1" HorizontalAlignment="Stretch" Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" IsDefault="True" Name="btnDialogOk" Click="btnDialogOk_Click">_OK</Button>
            <Button Grid.Column="0" IsDefault="True" Name="btnDialogYes" Click="btnDialogYes_Click" Visibility="Collapsed">_Ja</Button>
            <Button Grid.Column="1" IsCancel="True" Name="btnDialogCancel">_Abbrechen</Button>
            <Button Grid.Column="1" IsCancel="True" Name="btnDialogNo" Visibility="Collapsed">_Nein</Button>
        </Grid>
    </Grid>
    
</Window>
